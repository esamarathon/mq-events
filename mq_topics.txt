All exchanges are of type "topic", durable, auto-delete ON.
Per-Application receiving queues should have an expiry (x-expires property) to not clutter the server forever.
I'd suggest something like 4 hours, to bridge any kind of connectivity issues or outages.

exchange:
    cg
topics:
    <event_name>.obs.scene.<scene_name>.{start,end}{,.gamescene}
    <event_name>.run.changed
    <event_name>.sponsor.logo.changed
    <event_name>.timer.{started,paused,resumed,finished,reset,edited}
    <event_name>.timer.team.<teamID>.{finished,undid_finish}

exchange:
    bigbutton
topics:
    <button_id>.ip_addr
    <button_id>.pressed
    <button_id>.rgb
    <button_id>.tag_scanned

exchange:
    users
topics:
    user.<user_id>.updated

exchange:
    moderation
topics:
    screened.sub
    screened.tweet
    screened.cheer

exchange:
    tracker
topics:
    <event_name>.donation.<donation_id>.fully_processed
    <event_name>.donation_total.updated
